name: Issue 37 physical vwindow RED

# Stable-head RED gate: do not advance this branch until the regression commit lands.
on:
  push:
    branches:
      - issue-37-directional-prefetch-blank-viewport
    paths:
      - '.github/workflows/issue-37-directional-red.yml'
      - 'tools/apply_issue37_manual_scroll_follow_regression.py'

permissions:
  contents: write

jobs:
  red:
    runs-on: windows-latest
    timeout-minutes: 15
    steps:
      - name: Check out branch and submodules
        uses: actions/checkout@v4
        with:
          submodules: recursive
          fetch-depth: 0

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Set up .NET 10
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'

      - name: Install pinned Core dependencies
        shell: pwsh
        run: npm ci --prefix dependencies/AIConversationCore --no-audit --no-fund

      - name: Stage manual-scroll follow regression
        shell: pwsh
        run: python tools/apply_issue37_manual_scroll_follow_regression.py

      - name: Validate staged test patch
        shell: pwsh
        run: git diff --check

      - name: Build AgentPanelSpeaker Release
        shell: pwsh
        run: dotnet build AgentPanelSpeaker/AgentPanelSpeaker.csproj --configuration Release

      - name: Prove physical-window regressions RED
        shell: pwsh
        run: |
          $exe = Join-Path $env:GITHUB_WORKSPACE 'AgentPanelSpeaker\bin\Release\net10.0-windows10.0.22621.0\AgentPanelSpeaker.exe'
          $stdout = Join-Path $env:RUNNER_TEMP 'issue37-physical-red.stdout.txt'
          $stderr = Join-Path $env:RUNNER_TEMP 'issue37-physical-red.stderr.txt'
          $process = Start-Process -FilePath $exe `
            -ArgumentList '--test', 'virtual-window' `
            -RedirectStandardOutput $stdout `
            -RedirectStandardError $stderr `
            -PassThru -Wait
          $output = @()
          if (Test-Path $stdout) { $output += Get-Content $stdout }
          if (Test-Path $stderr) { $output += Get-Content $stderr }
          $output | ForEach-Object { Write-Host $_ }
          $text = $output -join "`n"
          if ($process.ExitCode -eq 0) {
            throw 'Expected physical #37 regressions to fail before production changes.'
          }
          foreach ($pass in @(
            'PASS  virtual-window/core-single-anchor-user-context-unit-is-preserved',
            'PASS  virtual-window/browser-is-bounded-convergent-and-diagnostics-are-bounded',
            'PASS  virtual-window/programmatic-playback-scroll-does-not-request-shift'
          )) {
            if (-not $text.Contains($pass)) {
              throw "Existing GREEN regression did not stay GREEN: $pass"
            }
          }
          foreach ($requiredRed in @(
            'FAIL  virtual-window/spacer-shift-restores-materialized-content',
            'FAIL  virtual-window/directional-prefetch-follows-scroll-direction',
            'FAIL  virtual-window/manual-scroll-disables-follow-before-vwindow-shift',
            'FAIL  virtual-window/physical-window-keeps-five-viewports-materialized',
            'FAIL  virtual-window/playback-voice-cursor-prefetches-inside-tall-turn'
          )) {
            if (-not $text.Contains($requiredRed)) {
              throw "Required physical RED regression did not fail: $requiredRed"
            }
          }
          $failLines = @($output | Where-Object { $_ -match '^FAIL  ' })
          if ($failLines.Count -ne 5) {
            throw "Expected exactly 5 physical RED failures, observed $($failLines.Count)."
          }
          if (-not $text.Contains('Physical vwindow retains a removable')) {
            throw 'Minimal-window regression failed for an unexpected reason.'
          }
          if (-not $text.Contains('Follow mode remained enabled')) {
            throw 'Manual-scroll follow regression failed for an unexpected reason.'
          }
          Write-Host 'RED-PROVEN issue-37 physical vwindow and manual-follow failures reproduced.'

      - name: Commit manual-scroll RED regression
        shell: pwsh
        run: |
          git config user.name 'chatgpt-codex-connector'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add AgentPanelSpeaker/Issue37VirtualWindowRegressionTestRunner.cs
          if (git diff --cached --quiet) { throw 'Manual-scroll regression patch produced no source change.' }
          git commit -m 'test: require manual scroll to disable follow (#37) [skip ci]'
          git push origin HEAD:issue-37-directional-prefetch-blank-viewport
          Write-Host 'Manual-scroll RED regression committed after intended failure was proven.'
