namespace AgentPanelSpeaker;

/// <summary>
/// Selects when Windows.Media speech uses explicit SSML bookmark timing.
/// </summary>
internal enum WindowsMediaBookmarkMode
{
  Off,
  Fallback,
  Always
}
